server:
    port: 0
spring:
    application:
        name: booking-service
    cloud:
        config:
            uri: http://localhost:8888
            profile: dev
eureka:
    instance:
        instance-id: ${spring.application.name}:${random.uuid}
management:
    endpoints:
        web:
            exposure:
                include: *